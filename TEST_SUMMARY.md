# VIP-Switch 测试方案总结

## 📌 测试方案概述

基于VIP-Switch实现计划，我已创建了一套完整的测试方案，**重点是通过日志输出验证功能，不执行实际的网络IP操作**。

---

## ✅ 已创建的测试文件

### 1. 测试文档

| 文件 | 说明 | 内容 |
|------|------|------|
| `TEST_PLAN.md` | 详细测试计划 | 10个测试用例、测试检查清单、性能指标、调试技巧 |
| `test/README.md` | 完整测试指南 | 快速开始、手动测试场景、日志分析、故障排查 |
| `test/QUICKSTART.md` | 5分钟快速开始 | 快速步骤、预期输出、常见问题 |

### 2. 测试Hook脚本（只做日志输出）

| 文件 | 功能 | 特点 |
|------|------|------|
| `test/scripts/on-master-test.sh` | 模拟Master绑定VIP | 输出"SIMULATING"标记，不执行真实ip命令 |
| `test/scripts/on-slave-test.sh` | 模拟Slave解绑VIP | 输出"SIMULATING"标记，不执行真实ip命令 |
| `test/scripts/on-ready-test.sh` | 节点启动初始化 | 输出VIP配置和环境变量 |
| `test/scripts/on-destroy-test.sh` | 节点关闭清理 | 输出清理操作和环境变量 |

**共同特点**：
- ✅ 只做日志输出
- ✅ 输出详细的模拟操作信息
- ✅ 打印接收到的环境变量
- ✅ 使用清晰的分隔线格式化输出
- ✅ 所有脚本都有执行权限

### 3. 测试配置文件

| 文件 | 说明 |
|------|------|
| `test/configs/vip-test.conf` | VIP测试配置（192.168.1.100/32, eth0）|
| `test/configs/node1-config.yaml` | 节点1配置（127.0.0.1:7946）|
| `test/configs/node2-config.yaml` | 节点2配置（127.0.0.1:7947）|
| `test/configs/node3-config.yaml` | 节点3配置（127.0.0.1:7948）|

**特点**：
- 使用本地回环地址（127.0.0.1）避免网络依赖
- 使用不同的端口（7946, 7947, 7948）避免冲突
- 配置了测试专用的Hook脚本路径
- 日志格式为text（方便查看）

### 4. 测试执行脚本

| 文件 | 功能 |
|------|------|
| `test/run-test.sh` | 自动化测试脚本，运行8个测试用例 |
| `test/manual-test.sh` | 手动测试辅助脚本，提供快速命令参考 |

---

## 🧪 测试用例清单

### 自动化测试（8个）

1. **单节点启动测试** - 验证ToReady事件触发
2. **单节点选举测试** - 验证自动选举为Master
3. **双节点集群测试** - 验证产生1个Master和1个Slave
4. **三节点集群测试** - 验证产生1个Master和2个Slave
5. **故障转移测试** - 验证Leader故障后自动选举新Leader
6. **环境变量测试** - 验证NODE_ID和EVENT_TYPE正确传递
7. **VIP配置读取测试** - 验证正确读取vip-test.conf
8. **优雅关闭测试** - 验证ToDestroy Hook正确执行

### 手动测试场景

- **场景1**: 单节点启动
- **场景2**: 双节点集群
- **场景3**: 三节点集群
- **场景4**: 故障转移
- **场景5**: 优雅关闭

---

## 🚀 快速开始

### 方式1：自动化测试（推荐）

```bash
# 1. 构建项目
make build

# 2. 运行自动化测试
./test/run-test.sh

# 3. 查看结果
# 预期看到：Total Tests: 8, Passed: 8, Failed: 0
```

### 方式2：手动测试

```bash
# 1. 启动手动测试辅助
./test/manual-test.sh

# 2. 在其他终端启动节点
./build/vip-switch --config ./test/configs/node1-config.yaml

# 3. 查看日志
tail -f ./test/logs/node1.log
```

---

## 📊 测试Hook脚本示例输出

### on-master-test.sh 输出

```
[ToMaster] Event received at Sat Jan 18 10:30:00 CST 2026
[ToMaster] Node ID: node1
[ToMaster] Event Type: ToMaster
[ToMaster] =============================================
[ToMaster] SIMULATING VIP BINDING (NO REAL IP OPERATION)
[ToMaster] Binding VIP: 192.168.1.100/32 on eth0
[ToMaster] =============================================
[ToMaster] Executing: ip addr replace 192.168.1.100/32 dev eth0
[ToMaster] ✓ VIP bind command executed successfully (simulated)
[ToMaster] Sending ARP broadcast (1/3): arping -U -c 1 -I eth0 192.168.1.100
[ToMaster] Sending ARP broadcast (2/3): arping -U -c 1 -I eth0 192.168.1.100
[ToMaster] Sending ARP broadcast (3/3): arping -U -c 1 -I eth0 192.168.1.100
[ToMaster] =============================================
[ToMaster] VIP bound successfully (simulated)
[ToMaster] Current state: MASTER with VIP 192.168.1.100/32
[ToMaster] =============================================
[ToMaster] Process info: PID=12345
[ToMaster] Environment variables:
[ToMaster]   NODE_ID=node1
[ToMaster]   EVENT_TYPE=ToMaster
[ToMaster]   VIP_ADDRESS=192.168.1.100/32
[ToMaster]   INTERFACE=eth0
```

### on-slave-test.sh 输出

```
[ToSlave] Event received at Sat Jan 18 10:30:00 CST 2026
[ToSlave] Node ID: node2
[ToSlave] Event Type: ToSlave
[ToSlave] =============================================
[ToSlave] SIMULATING VIP UNBINDING (NO REAL IP OPERATION)
[ToSlave] Unbinding VIP: 192.168.1.100/32 from eth0
[ToSlave] =============================================
[ToSlave] Executing: ip addr del 192.168.1.100/32 dev eth0
[ToSlave] ✓ VIP unbind command executed successfully (simulated)
[ToSlave] Note: Command would have failed if VIP didn't exist (ignored)
[ToSlave] =============================================
[ToSlave] VIP unbound successfully (simulated)
[ToSlave] Current state: SLAVE (VIP unbound)
[ToSlave] =============================================
```

---

## ✨ 测试方案特点

### 1. 不执行实际网络操作

所有测试Hook脚本都明确标注：
```bash
[ToMaster] SIMULATING VIP BINDING (NO REAL IP OPERATION)
[ToMaster] ✓ VIP bind command executed successfully (simulated)
```

**不会真正执行**：
- ❌ `ip addr replace`
- ❌ `ip addr del`
- ❌ `arping -U`

### 2. 完整的日志验证

通过日志验证所有功能：
- ✅ Hook事件触发（ToReady, ToMaster, ToSlave, ToDestroy）
- ✅ 状态机转换（Ready → Master/Slave）
- ✅ Raft选举（单节点、双节点、三节点）
- ✅ 故障转移（Leader故障后自动选举新Leader）
- ✅ 环境变量传递（NODE_ID, EVENT_TYPE, VIP_ADDRESS等）
- ✅ VIP配置读取（从vip-test.conf）

### 3. 自动化和手动测试结合

- **自动化测试**：一键运行8个测试用例，自动判断通过/失败
- **手动测试**：灵活测试各种场景，方便调试

### 4. 完善的文档

- **TEST_PLAN.md**: 详细的测试计划和理论说明
- **test/README.md**: 完整的测试指南和故障排查
- **test/QUICKSTART.md**: 5分钟快速开始指南

---

## 📈 性能指标

| 指标 | 目标值 | 测量方法 |
|------|--------|----------|
| 节点启动时间 | < 5秒 | 从启动到ToReady完成 |
| 选举收敛时间 | < 3秒 | 从集群形成到Leader确定 |
| Hook执行时间 | < 1秒 | Hook脚本执行完成 |
| 故障转移时间 | < 5秒 | Leader故障到新Leader产生 |
| 日志延迟 | < 100ms | Hook输出到日志的时间 |

---

## 🎯 测试覆盖范围

### 功能验证
- [x] ToReady 事件触发
- [x] ToMaster 事件触发
- [x] ToSlave 事件触发
- [x] ToDestroy 事件触发
- [x] 状态机转换（Ready → Master/Slave）
- [x] Raft领导者选举
- [x] 故障自动转移
- [x] 防抖动机制
- [x] 优雅关闭

### 环境变量验证
- [x] NODE_ID 正确传递
- [x] EVENT_TYPE 正确传递
- [x] VIP_ADDRESS 从vip.conf正确读取
- [x] INTERFACE 从vip.conf正确读取
- [x] 所有环境变量在日志中输出

### 日志验证
- [x] Hook stdout实时输出到日志
- [x] Hook stderr实时输出到日志
- [x] 日志格式正确（text）
- [x] 日志级别正确（info）

### 异常处理
- [x] Hook超时控制（配置了timeout）
- [x] Hook失败策略（配置了on_failure）
- [x] 节点故障自动恢复
- [x] 配置文件缺失报错
- [x] Hook脚本缺失报错

---

## 🔍 日志分析命令

### 查看特定事件

```bash
# 搜索所有ToMaster事件
grep "\[ToMaster\]" ./test/logs/*.log

# 搜索所有ToSlave事件
grep "\[ToSlave\]" ./test/logs/*.log

# 搜索状态转换
grep "State transition" ./test/logs/*.log
```

### 统计Master/Slave数量

```bash
# 统计Master数量
grep -c "\[ToMaster\] VIP bound successfully" ./test/logs/*.log

# 统计Slave数量
grep -c "\[ToSlave\] VIP unbound successfully" ./test/logs/*.log
```

### 找到当前Leader

```bash
# 找到执行了ToMaster的节点
grep -l "\[ToMaster\] VIP bound successfully" ./test/logs/*.log
```

---

## 🛠️ 故障排查

### 常见问题

1. **Binary not found**
   - 解决方案：`make build`

2. **端口被占用**
   - 解决方案：`pkill -f "vip-switch"`

3. **Hook脚本权限错误**
   - 解决方案：`chmod +x ./test/scripts/*.sh`

4. **日志文件未生成**
   - 解决方案：`mkdir -p ./test/logs`

5. **Raft选举超时**
   - 解决方案：清理数据 `rm -rf ./test/data/*`，检查防火墙

---

## 📝 测试报告模板

```markdown
# VIP-Switch 测试报告

**测试日期**: 2026-01-18
**测试人员**: [姓名]
**测试版本**: [版本号]

## 测试结果概览
- 总测试用例: 8
- 通过: 8
- 失败: 0
- 通过率: 100%

## 详细测试结果

| 测试用例 | 状态 | 说明 |
|----------|------|------|
| 单节点启动测试 | ✅ | ToReady和ToMaster正确触发 |
| 单节点选举测试 | ✅ | 自动选举为Master |
| 双节点集群测试 | ✅ | 产生1个Master和1个Slave |
| 三节点集群测试 | ✅ | 产生1个Master和2个Slave |
| 故障转移测试 | ✅ | Leader故障后自动选举新Leader |
| 环境变量测试 | ✅ | NODE_ID和EVENT_TYPE正确传递 |
| VIP配置读取测试 | ✅ | 正确读取vip-test.conf |
| 优雅关闭测试 | ✅ | ToDestroy正确执行 |

## 发现的问题
无

## 建议和改进
1. 可以添加更多边界测试用例
2. 可以添加性能监控和指标收集

## 结论
✅ 所有测试通过，系统功能正常
```

---

## 📚 相关文档

- **VIP-Switch README**: `README.md` - 项目主文档
- **实现计划**: `.opencode/tasks/task1-implementation-plan.md` - 完整实现计划
- **详细测试计划**: `TEST_PLAN.md` - 测试理论和用例设计
- **测试指南**: `test/README.md` - 完整的测试操作指南
- **快速开始**: `test/QUICKSTART.md` - 5分钟快速测试

---

## ✨ 总结

### 已完成

✅ 创建了完整的测试方案文档（3个文档文件）
✅ 创建了4个测试Hook脚本（只做日志输出）
✅ 创建了4个测试配置文件（1个VIP配置 + 3个节点配置）
✅ 创建了2个测试执行脚本（自动化 + 手动辅助）
✅ 设计了8个自动化测试用例
✅ 提供了5个手动测试场景
✅ 所有脚本都有执行权限
✅ 使用本地回环地址，避免网络依赖

### 测试方案特点

🎯 **不执行实际网络操作** - 只做日志输出
🎯 **完整的日志验证** - 验证所有核心功能
🎯 **自动化和手动结合** - 灵活的测试方式
🎯 **完善的文档** - 从理论到实践的完整指南

### 可以开始测试

现在你可以：

1. **快速测试**：运行 `./test/run-test.sh`
2. **手动测试**：使用 `./test/manual-test.sh`
3. **查看文档**：阅读 `test/QUICKSTART.md` 或 `test/README.md`

**祝测试顺利！ 🎉**
